/**
 * Instrument Tracker — configuration.
 *
 * This is the ONLY file you need to edit to connect the app to your Sheet.
 *
 * Paste the web app URL you got from Apps Script when you clicked
 * Deploy → New deployment → Web app. It looks like:
 *
 *   https://script.google.com/macros/s/AKfycb................/exec
 *
 * It must end in /exec (not /dev). Keep the quote marks around it.
 *
 * Step-by-step instructions are in README.md.
 */

var CONFIG = {
  API_URL: 'PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE',

  /** Shown in the header and printed at the bottom of every QR label. */
  ORGANISATION: 'BAPS London Mandir',
  APP_NAME: 'Instrument Tracker'
};
